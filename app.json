{
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "ACCESS_TOKEN": {
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "VERIFY_TOKEN": {
      "required": true
    },
    "ANSIBLE_ROLES_PATH": {
      "required": true,
      "value": "ansible"
    },
  },
  "name": "mako",
  "repository": "https://github.com/mako-ai/mako",
  "scripts": {
    "postdeploy": "ansible-galaxy install mako-ai.messenger-bot-test-pipeline && cd $ANSIBLE_ROLES_PATH && ansible-playbook -i inventory postdeploy.yml"
  },
  "website": "https://mako-ai.github.io/"
}
