{
  "buildpacks": [
    {
      "url": "heroku/python"
    },
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "ACCESS_TOKEN": {
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "VERIFY_TOKEN": {
      "required": true
    }
  },
  "name": "mako",
  "repository": "https://github.com/mako-ai/mako",
  "scripts": {
    "postdeploy": "ansible-galaxy install mako-ai.messenger-bot-test-pipeline && ansible-playbook -i ansible/inventory ansible/site.yml"
  },
  "website": "https://mako-ai.github.io/"
}
